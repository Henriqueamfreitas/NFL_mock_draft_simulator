import styled from "styled-components";

export const StyledPlayersForm = styled.form`
    background-color: rgb(250,250,250);
    padding: 0 .5rem .5rem .5rem;
    display: flex;
    justify-content: space-between;
    max-width: 100%;
    flex-wrap: wrap;
    gap: .25rem;
    row-gap: .5rem;
`